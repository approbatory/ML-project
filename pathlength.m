%compute the total path length of a 2d path and approximately divide it up
%into N path segments of equal path length each, only limited by the finite
%sampling rate.
function [len, classed] = pathlength(path, N)
%TODO write this
end